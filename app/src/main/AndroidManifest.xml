<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.android.laudeni.beautypoint">


<uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- Splash screen -->
        <activity
            android:label="@string/app_name"
            android:name="br.com.android.laudeni.beautypoint.TelaDeInicio"
            android:screenOrientation="portrait"
            android:theme="@style/AppFullScreenTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Principal -->
        <activity
            android:label="@string/app_name"
            android:name="br.com.android.laudeni.beautypoint.MainActivity"/>

        <activity android:name=".Salao" />
        <activity android:name=".DetalheSalao" />
    </application>


</manifest>